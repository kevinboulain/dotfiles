;;; agda.el --- Agda 2 language support.

;;; Commentary:

;;; Nothing special here, `adga2' mode should be installed along the compiler.

;;; Code:

(when (require 'agda2 nil t))

;;; agda.el ends here
